@model RPGSystem.Models.CharacterSheetViewModel

<h1>@Model.Character.Name</h1>

<h3>Stats</h3>

@if (Model.RollResult != null)
{
    <h3>Roll Result</h3>

    <p>
        @Model.RollResult.StatName Roll:
        @Model.RollResult.DiceRoll
        +
        @Model.RollResult.Modifier
        =
        @Model.RollResult.Total
    </p>
}

<div>
    <p>
        Strength: @Model.Character.Strength
        (Modifier: @Model.Character.GetStrengthModifier())

        <form asp-action="RollStat" method="post">
            <input type="hidden" name="statName" value="Strength" />
            <button type="submit">Roll</button>
        </form>
    </p>

    <p>
        Dexterity: @Model.Character.Dexterity
        (Modifier: @Model.Character.GetDexterityModifier())
    </p>

    <p>
        Constitution: @Model.Character.Constitution
        (Modifier: @Model.Character.GetConstitutionModifier())
    </p>
</div>